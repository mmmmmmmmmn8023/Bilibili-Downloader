fix(ci): 部署前先清理服务器克隆目录的本地改动，避免 git pull 因本地修改中止

服务器 /var/www/deploy-bilibili.sh 为手动放置的旧副本，可能使用 git pull（merge）
而非 reset --hard；当克隆目录存在未提交改动（如 db.py/deploy.sh）时，pull 会
报 “would be overwritten by merge” 并中止整个部署。

在 deploy.yml 的 SSH 调用中，先 cd 到克隆目录执行 git fetch + checkout -- . +
git reset --hard origin/develop/feature 清理本地改动并对齐远端，再调用部署脚本。
这样无论服务器脚本是新版（reset）还是旧版（pull）都能顺利部署，且后续不再复发。
DIR/BRANCH 与 deploy-bilibili.sh 保持一致。
